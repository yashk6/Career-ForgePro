

services:

  backend:
    build: ./server
    ports:
      - "5000:5000"
    volumes:
      - ./server:/app
    environment:
      - NODE_ENV=development

  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    depends_on:
      - backend